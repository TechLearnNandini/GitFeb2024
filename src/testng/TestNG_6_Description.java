package testng;

import org.testng.annotations.Test;
import org.testng.annotations.BeforeTest;
import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.testng.annotations.AfterTest;

//public class TestNG_6_Description {
/*	WebDriver driver;
	@Test
	public void dm2() {
		
		
	}
	
	@Test(timeOut=7000)
	public void googleTestNG() throws InterruptedException {
		
	
		driver.findElement(By.xpath("//li[@data-testid='headerArticlesMenuButton']")).click();
		Thread.sleep(2000);
		driver.findElement(By.xpath("//body/div[@id='app']/header[@class='top-header']/div[@class='container']/div[@class='row no-gutters']/div[@class='header-items col']/div[@class='d-none d-lg-flex navigation-block']/div/div[1]/div[1]/div[1]/div[1]/div[2]/div[2]/div[1]/a[1]")).click();
		
	}
	
	
	
 
  @BeforeTest
  public void beforeTest() {
	  driver = new ChromeDriver();
		driver.manage().window().maximize();
		driver.get("https://www.dummies.com/");
  }

  @AfterTest
  public void afterTest() {
	  driver.quit();
  }

}*/
